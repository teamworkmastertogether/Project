@model IEnumerable<ChatApp.Models.Dto.PostDto>

@{
    ViewBag.Title = "GetPosts";
    Layout = "~/Views/Admin/_AdminPartialView.cshtml";
}

<div class="content-wrapper" style="padding-left: 10px">
    <section class="content-header">
        <h1>
            Danh sách bài đăng
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active">Posts</li>
        </ol>
    </section>
    <div class="add-student" style="padding-bottom: 15px; border-bottom: 1px solid gray; margin-bottom: 10px">
    </div>
    <table class="table table-striped table-hover" id="table-student">
        <thead>
            <tr>
                <th>Mã bài đăng</th>
                <th>
                    Tên thành viên
                </th>
                <th>
                    Avatar
                </th>
                <th>
                    Nội dung
                </th>
                <th>
                    Nhóm
                </th>
                <th>
                    Thời gian đăng
                </th>
                <th>
                    Số lượt like
                </th>
                <th>Thao tác</th>
            </tr>
        </thead>

        @*<tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PostId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NameOfUser)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.avatar)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PostText)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GroupName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TimePost)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LikeNumber)
                </td>
                <td>
                    <a href="#"><button class="btn btn-danger">Chặn</button></a>
                </td>
            </tr>
            }
        </tbody>*@
    </table>
</div>
